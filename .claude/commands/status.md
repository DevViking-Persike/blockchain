Show the full status of the running blockchain node.

Run curl against these endpoints and display results:
1. `GET /api/node/info` - Node configuration
2. `GET /api/chain/valid` - Chain validity
3. `GET /api/transactions/pending` - Pending transactions count
4. `GET /api/peers` - Connected peers
