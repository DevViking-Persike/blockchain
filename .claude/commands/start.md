Start the blockchain node in release mode.

Run `cargo run --release -p blockchain-node` in the background and verify it started by hitting the `/api/node/info` endpoint with curl. Show the node info response.
