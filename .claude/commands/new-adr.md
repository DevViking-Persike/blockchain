Create a new Architecture Decision Record (ADR).

1. List existing ADRs in `docs/adr/` to determine the next number
2. Ask the user for the ADR title
3. Create the ADR file in `docs/adr/` following the template format in `docs/adr/0000-template.md`
4. Use the format `NNNN-title-in-kebab-case.md`
