[package]
name = "blockchain-vm"
version = "0.1.0"
edition = "2021"

[dependencies]
blockchain-core = { path = "../blockchain-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
thiserror = "2"
tracing = "0.1"
chrono = "0.4"
